language: node_js
node_js:
  - '14'
before_script:
  - npm install gulp
  - npm install gulp --save-dev
  - npm i gulp-inline-source 
script: gulp inlinesource
after_script:
  - git add .
  - git commit -m "build"
  - git push
